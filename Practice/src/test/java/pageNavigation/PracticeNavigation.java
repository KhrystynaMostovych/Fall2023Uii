package pageNavigation;
/*
import io.github.bonigarcia.wdm.WebDriverManager;
import org.junit.After;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import utillities.Driver;

import java.util.concurrent.TimeUnit;

public class PracticeNavigation {

    WebDriver driver;

    @Before
    public void setUp(){
        driver = Driver.getDriver();}
    @After
    public void tearDown(){
        Driver.closeDriver();
    }

    @Test
    public void practicePageNavigate(){
driver.navigate().to("http://www.practiceselenium.com/");
        WebElement teaLink = driver.findElement(By.xpath("//a[contains(@data-title'Tea Link'"));
        teaLink.click();
WebElement parantOfTeaLink = teaLink.findElement(By.xpath("./.."));
        Assert.assertTrue(parantOfTeaLink.getAttribute("class").equals("active"));
driver.navigate().back();

// verify welcome is selected by default
         WebElement welcomeLink = driver.findElement(By.xpath("//a[contains(@data-title'Tea Link'"));
         Assert.assertTrue(welcomeLink.getAttribute("class").equals("active"));
  driver.navigate().forward();
  WebElement inputName = driver.findElement(By.xpath(""));
  inputName.sendKeys("...");
  driver.navigate().refresh();
  inputName.sendKeys("...");


     }

    }
*/
